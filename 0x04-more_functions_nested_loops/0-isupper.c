#include "main.h"
#include <ctype.h>
/**
 * _isupper - uppercase letters
 * @c: character to check
 *
 * Return: 0 or 1
 */
int _isupper(int c)
	a=1;
  	b=0;

{
	if (c >= 'A' && c <= 'Z')
  	putchar (a);
	else
	putchar (b);
	return(0);
}
